import createShortURLFactory from './create-url'

describe('url', () => {
    describe('test createURL', () => {
        describe('given a valid URL ', () => {
            it('must save it and return an ID ', () => {
                console.log('....')
            })
        })
    })
})
